# BrightPath Kids Web App

If `npm` is not found, use the included `START_HERE.command` file.

## Start The App

Double-click:

```text
START_HERE.command
```

Or run:

```bash
/Users/karthikbollam/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm install
/Users/karthikbollam/.cache/codex-runtimes/codex-primary-runtime/dependencies/bin/pnpm run dev
```

## If You Want Normal npm Commands

Install Node.js from:

```text
https://nodejs.org
```

After that, regular commands like `npm install` and `npm run dev` will work in Terminal.
